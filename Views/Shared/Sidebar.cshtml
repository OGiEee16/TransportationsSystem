<aside class="sidebar">
    <div class="p-3">
        <!-- Logo/Brand -->
        <div class="text-center mb-4 pb-3 border-bottom border-secondary">
        <h4 class="mb-0">
    <i class="fas fa-bus text-warning me-2"></i>
     <span class="fw-bold">Gr-TranspoStation</span>
       </h4>
   </div>

   <!-- Navigation Menu -->
        <nav class="nav flex-column">
       @if (User?.Identity?.IsAuthenticated ?? false)
  {
        @if (User.IsInRole("ADMIN"))
      {
    <!-- Admin Menu -->
  <div class="mb-3">
<small class="text-muted ps-2 mb-2 d-block">ADMIN PANEL</small>
      <a class="nav-link" href="@Url.Action("Index", "Admin")">
     <i class="fas fa-tachometer-alt me-2"></i> Dashboard
     </a>
     <a class="nav-link" href="@Url.Action("Bookings", "Admin")">
  <i class="fas fa-calendar-check me-2"></i> Manage Bookings
   </a>
   <a class="nav-link" href="@Url.Action("Users", "Admin")">
   <i class="fas fa-user-friends me-2"></i> Manage Users
  </a>
 <a class="nav-link" href="@Url.Action("Drivers", "Admin")">
  <i class="fas fa-users me-2"></i> Manage Drivers
    </a>
         </div>
   }
     else if (User.IsInRole("DRIVER"))
           {
     <!-- Driver Menu -->
  <div class="mb-3">
            <small class="text-muted ps-2 mb-2 d-block">DRIVER PORTAL</small>
    <a class="nav-link" href="@Url.Action("Index", "Driver")">
  <i class="fas fa-tachometer-alt me-2"></i> Dashboard
       </a>
    <a class="nav-link" href="@Url.Action("MyTrips", "Driver")">
   <i class="fas fa-route me-2"></i> My Trips
   </a>
      </div>
           }
    else
    {
      <!-- User Menu -->
      <div class="mb-3">
      <small class="text-muted ps-2 mb-2 d-block">USER MENU</small>
   <a class="nav-link" href="@Url.Action("Index", "Home")">
       <i class="fas fa-home me-2"></i> Home
         </a>
            <a class="nav-link" href="@Url.Action("Create", "Booking")">
        <i class="fas fa-plus-circle me-2"></i> New Booking
     </a>
            <a class="nav-link" href="@Url.Action("MyBookings", "Booking")">
          <i class="fas fa-list me-2"></i> My Bookings
    </a>
     </div>
 }
            }
            else
          {
         <!-- Guest Menu -->
       <div class="mb-3">
<small class="text-muted ps-2 mb-2 d-block">WELCOME</small>
    <a class="nav-link" href="@Url.Action("Index", "Home")">
        <i class="fas fa-home me-2"></i> Home
  </a>
         <a class="nav-link" href="@Url.Action("Login", "Account")">
        <i class="fas fa-sign-in-alt me-2"></i> Login
</a>
         <a class="nav-link" href="@Url.Action("Register", "Account")">
      <i class="fas fa-user-plus me-2"></i> Register
          </a>
    </div>
     }
     </nav>
    </div>
</aside>
